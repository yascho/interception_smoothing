from setuptools import setup

setup(name='interception_smoothing',
      version='0.1.0',
      description='Gray-box Certificates for Graph Neural Network',
      packages=['interception_smoothing'])